# KClip v0.1.10

Color clip sizing follow-up release.

## Highlights

- Made compact `Color` clips fill the full preview slot so the color block no longer looks undersized inside tray cards.
- Enlarged the color surface in edit and expanded preview flows so the swatch is easier to read at a glance.
- Kept the animated color transitions and the picker-sync stability fix from the previous follow-up patch.

## Validation

- `swift test`
- `./script/build_and_run.sh --verify`
- `./script/make_release.sh v0.1.10`
