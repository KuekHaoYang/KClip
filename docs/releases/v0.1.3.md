# KClip v0.1.3

Compact image geometry release.

## Highlights

- Corrected the compact image preview radius so it now follows `inner = outer - padding`.
- Kept explicit fade-and-scale transitions for image preview motion.
- Preserved the cleaner media surface with no redundant in-preview `Image` badge.

## Validation

- `swift test`
- `./script/build_and_run.sh --verify`
- `./script/make_release.sh v0.1.3`
